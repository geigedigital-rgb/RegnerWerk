[build]
builder = "NIXPACKS"
buildCommand = "npm run build"

[deploy]
startCommand = "npx next start --hostname 0.0.0.0 --port ${PORT:-8080}"
healthcheckPath = "/konfigurator"
healthcheckTimeout = 60
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
